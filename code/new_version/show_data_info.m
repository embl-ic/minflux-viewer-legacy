function show_data_info (data)

    disp("show data info reached");
   
    disp("selected data file name:" + data.file.name);

    disp("selected data file path:" + data.file.folder);

    disp("selected data file datetime:" + data.file.datetime);

end